---
status: Final
reviewer: "SDD review gate — independent reviewer subagent (pass 4) + host synthesis"
date: "2026-07-04"
review_pass: 4
feature_size: "M"
route: "standard"
scope: "Post pass-3 test matrix expansion (no git repository)"
gate_result: "CHANGES REQUESTED"
---

# Review — weather-explorer (2026-07-04, pass 4)

## Scope

- **Diff:** Full tree after pass-3 test matrix expansion (90 tests).
- **Build gate:** `npm test` — **90/90 passed** (29 files); lint + tsc green.
- **Prior:** Pass 3 CHANGES REQUESTED on test-plan gaps; pass-3 implement added 17 tests.

## Pass 3 → Pass 4 delta

| Area | Pass 3 | Pass 4 |
|---|---|---|
| UI AC test chain | Fail (12+ gaps) | **Partial** — 24/26 ACs have ≥1 test |
| AC-12 (no auth gate) | Missing | **Still missing** |
| AC-14 (calm failure UI) | Missing | **Still missing** |
| AC-04/09/16/18 assertions | Partial | **Partial** — deeper field checks still open |
| Core implementation | Pass | **Pass** |
| BFF + contract + lib | Pass | **Pass** |

## Findings

### Stage 1 — spec / AC compliance (blocking)

- **[stage-1] AC-12 has zero automated coverage** — n/a; AC: AC-12; problem: no test asserts anonymous access across MVP surfaces without login; suggested: smoke flow exercising search/map/forecast/compare/geolocation with no auth gate.

- **[stage-1] AC-14 forecast failure UI untested** — `components/weather/forecast/forecast-panel.tsx:27`; AC: AC-14; problem: error branch with calm retry exists but no test; suggested: component test with `forecastStatus: "error"`.

- **[stage-1] AC-04 day-card fields only partially asserted** — `components/weather/forecast/forecast-panel.test.tsx:51`; AC: AC-04; problem: icons + weekend only; weekday/°C/precip/wind/comfort badge not asserted; suggested: extend loaded-state test.

- **[stage-1] AC-09 compare metrics not asserted** — `components/weather/compare/compare-panel.test.tsx:39`; AC: AC-09; problem: labels/buttons only, not temp/precip/comfort values; suggested: assert fixture values.

- **[stage-1] AC-16 responsive breakpoints untested** — `components/weather/shell/weather-shell.test.tsx:14`; AC: AC-16; problem: hero-only; md/xl grid classes not asserted; suggested: assert layout class tokens.

- **[stage-1] AC-18 missing component-level test** — `lib/scoring/comfort.test.ts`; AC: AC-18; problem: unit only; no DOM badge assertion; suggested: forecast-panel comfort badge test.

- **[stage-1] AC-01 suggestion metadata not verified in UI** — `tests/e2e-ui/smoke.test.tsx:43`; AC: AC-01; problem: smoke checks fetch only, not region/country/flag in listbox; suggested: assert option text.

- **[stage-1] AC-05 visitor TZ contrast untested** — `lib/scoring/comfort.test.ts:57`; AC: AC-05; problem: no boundary case with visitor TZ ≠ location TZ; suggested: add contrasting instant test.

- **[stage-1] AC-13 animated background not asserted** — `components/weather/background/weather-background.test.tsx:15`; AC: AC-13; problem: clock/theme only; gradient/animation not checked; suggested: assert gradient by condition.

- **[stage-1] Malformed URL → empty hero untested** — `components/weather/state/weather-context.tsx:102`; AC: AC-16 edge; problem: unit codec only; suggested: provider mount with invalid params.

- **[stage-1] AC-03 outcome weakly asserted** — `components/weather/search/city-search.test.tsx:52`; AC: AC-03; problem: textbox value only, not forecast trigger; suggested: assert fetchForecast called.

### Stage 2 — quality

- **[stage-2] Smoke harness mocks client, not BFF replay** — `tests/e2e-ui/smoke.test.tsx:12`; problem: test-plan integration strategy differs; suggested: document as composed UI harness or wire route replay.

- **[stage-2] AC-06b orphan-marker guard not verified** — `weather-map-inner.test.tsx:103`; AC: AC-06b; problem: retry tested but marker count not asserted; suggested: assert marker unchanged after fail.

- **[stage-2] Compare panel error branch untested** — `compare-panel.tsx:49`; problem: no test for fetch rejection; suggested: mock rejected compare fetch.

- **[stage-2] Visual-regression and NFR load rows open** — `test-plan.md:81`; problem: CI/deploy items not in Vitest; suggested: track as ship CI follow-ups, not MVP unit gate.

## Positive observations

- Pass-3 expansion closed the major test-chain gaps (AC-03/04b/06/06b/07/08/09/10b/11b + chrome).
- 90 tests green; `lib/` framework-free; BFF + ajv contract coverage solid.
- No regressions in pass-2 code fixes.

## Finding resolutions

| Cluster | Verdict | Notes |
|---|---|---|
| AC-12 + AC-14 (zero coverage) | **Pending** | Hard blockers |
| Partial assertion gaps (AC-04/09/16/18/13/01/03/05) | **Pending** | Strengthen tests |
| Stage-2 items | **Pending** | Mostly non-blocking |
| NFR/visual-regression | **Pending** | Likely defer to CI |

## Gate result

**CHANGES REQUESTED** — Substantial progress since pass 3; **AC-12 and AC-14 remain hard gaps**. Remaining items are mostly partial assertions and stage-2 polish.

## Pass 4 remediation (2026-07-04, scope: AC-12 + AC-14 only)

| Finding | Resolution | Evidence |
|---|---|---|
| AC-12 zero coverage | **Fixed** | `tests/e2e-ui/smoke.test.tsx` — full `WeatherApp` render; asserts no auth/subscription copy; search, geo, pin, compare, map surfaces present |
| AC-14 forecast failure UI | **Fixed** | `components/weather/forecast/forecast-panel.test.tsx` — error state shows «Прогноз тимчасово недоступний» + «Повторити» calls `reloadForecast` |
| Partial assertion gaps | **Deferred** | User chose minimal scope; AC-04/09/16/18/13/01/03/05 remain open |
| NFR/visual-regression | **Not an issue** | Deferred to post-deploy CI (MVP) |

**Post-remediation gate:** `npm test` — **92/92 passed**; lint + tsc + build green.

**Re-review recommended** to confirm PASS on hard blockers; partial gaps may still yield stage-1 notes unless waived.
